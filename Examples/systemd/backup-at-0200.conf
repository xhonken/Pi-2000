# Example drop-in for win2k-backup.timer (server local time).
# Reset the existing schedule before adding a replacement.
[Timer]
OnCalendar=
OnCalendar=*-*-* 02:00:00
RandomizedDelaySec=15m
Persistent=true
