const fs=require('node:fs'),vm=require('node:vm'),assert=require('node:assert/strict');
const context={window:{},Math,Number,Error};vm.runInNewContext(fs.readFileSync('assets/cad.js','utf8'),context);const {analyze,calculate}=context.window.Win2kCAD;
assert.equal(calculate('sqrt(300^2+400^2)'),500);assert.equal(calculate('-2^2'),-4);assert.equal(calculate('2^-2'),.25);assert.ok(Math.abs(calculate('sin(30)')-.5)<1e-10);assert.equal(calculate('2*(3+4)'),14);assert.throws(()=>calculate('1/0'));assert.throws(()=>calculate('window.alert(1)'));
let result=analyze({width:300,height:200,frame:20,holes:[{x:10,y:10,d:8}]});assert.equal(result.checks[0].problems.length,0);assert.equal(result.checks[0].clearance,6);assert.equal(result.gross,18400);
result=analyze({width:100,height:100,frame:10,holes:[{x:50,y:50,d:8},{x:1,y:1,d:8},{x:2,y:2,d:8}]});assert.ok(result.checks[0].problems.length);assert.ok(result.checks[1].problems.length);assert.ok(result.checks[2].problems.length>1);assert.equal(result.net,null);assert.throws(()=>analyze({width:100,height:100,frame:50,holes:[]}));console.log('PASS: arithmetic parser, trigonometry, dimensions, frame area, edge/void/overlap checks');
