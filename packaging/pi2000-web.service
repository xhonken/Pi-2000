[Unit]
Description=Pi-2000 HTTPS frontend
After=network-online.target pi2000-admin.service
Wants=network-online.target
[Service]
Type=notify
User=caddy
Group=caddy
ExecStart=/usr/bin/caddy run --config /etc/pi2000web/Caddyfile --adapter caddyfile
ExecReload=/usr/bin/caddy reload --config /etc/pi2000web/Caddyfile --adapter caddyfile --address unix//var/lib/caddy/pi2000-admin/control.sock
Restart=on-failure
AmbientCapabilities=CAP_NET_BIND_SERVICE
CapabilityBoundingSet=CAP_NET_BIND_SERVICE
NoNewPrivileges=true
PrivateTmp=true
ProtectHome=true
ProtectSystem=strict
ReadWritePaths=/var/lib/caddy
[Install]
WantedBy=multi-user.target
